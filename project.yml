name: Camera
options:
  bundleIdPrefix: com.example
  deploymentTarget:
    macOS: "14.0"
targets:
  Camera:
    type: application
    platform: macOS
    sources:
      - Sources/Mac拍照软件
    settings:
      INFOPLIST_FILE: Sources/Mac拍照软件/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.example.camera
      PRODUCT_NAME: Camera
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      MARKETING_VERSION: "1.0"
      CURRENT_PROJECT_VERSION: 1
    files:
      - Sources/Mac拍照软件/Assets.xcassets
